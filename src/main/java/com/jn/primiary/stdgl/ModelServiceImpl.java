package com.jn.primiary.stdgl;

import org.springframework.stereotype.Service;

@Service
public class ModelServiceImpl {
    /*
	@Autowired
    private EsModelRepository modelRepository;
  
    public void save(FieldEntity poem) {
    	modelRepository.save(poem);
    }
  
    public Page<FieldEntity> search(String title, String content,String enname, Pageable pageable) {
        //return modelRepository.findDistinctByFieldnameContainingOrFieldcodeContainingOrEnnameContaining(title,content,enname,pageable);
        return modelRepository.findByFieldnameLikeOrFieldcodeLikeOrEnnameLike(title,content,enname,pageable);
    }

//    public Page<FieldEntity> search(String content, Pageable pageable) {
//        return modelRepository.findByContentLike(content,pageable);
//    }

    public Page<FieldEntity> findAll(Pageable pageable) {
        return modelRepository.findAll(pageable);
    }
    */
}
